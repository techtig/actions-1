name: My First Workflows

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welome message
        run: echo "My First GitHub Actions job"

        -name: list files
        run: ls

        - name: Read file
        run: cat README.md